[project]
name = "Stacks-vault"
version = "1.0.0"
description = "Lock STX and mint proof NFT on Stacks Layer 2 Bitcoin"
authors = ["dnapog"]
telemetry = false
cache_dir = ".cache"
requirements = []
boot_contracts = []

[contracts.Stacks-vault]
path = "Stacks-vault.clar"
clarity_version = 4
epoch = 4.0

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
